// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  // This is the most basic configuration. We can add more options later if needed.
  server: {
    open: true, // Automatically open the app in the browser when you start the dev server
  },
});